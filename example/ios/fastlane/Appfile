app_identifier(ENV["APP_IDENTIFIER"] || "com.reteno.example-app")
team_id(ENV["FASTLANE_TEAM_ID"]) unless ENV["FASTLANE_TEAM_ID"].to_s.strip.empty?
apple_id(ENV["MATCH_USERNAME"]) unless ENV["MATCH_USERNAME"].to_s.strip.empty?
